# RAG AI Frontend

This repository contains the frontend UI for a RAG-based AI Assistant.

## Features
- AI chat-style interface
- Connects to a Flask backend API
- Displays grounded answers with video references

## Note
The backend uses a local LLM (Ollama), so the public UI works only when the backend is running.

